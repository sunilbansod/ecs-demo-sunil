version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "sample-website"
        LoadBalancerInfo:
          ContainerName: "sunilcontainer001"
          ContainerPort: 80
